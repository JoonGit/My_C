#include <stdio.h>

int main(void) {
	//char szName[12] = { "Hello" };
	//gets_s(szName, sizeof(szName));
	

	//// 버퍼 확인하기
	// 01 = '\n'
	//FILE* fp = stdin;
	//getchar();
	//getchar();
	//getchar();
	//getchar();

	// scanf_s
	//int nData = 0;
	//scanf_s("%d", &nData);
	
	//FILE* fp = stdin;
	//int nAge = 0;
	//printf("나이를 입력 하세요");
	//scanf_s("%d", &nAge);

	//fflush(stdin);

	//int szName[12] = {0};
	//printf("이름를 입력 하세요");
	//gets_s(szName, sizeof(szName));
	return 0;
}