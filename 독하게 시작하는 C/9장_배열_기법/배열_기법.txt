버블정렬(2중 for문)
매번 조건에 맞는 변수를 교환

- 선택정렬(선택 변수를 통해 교환)
모든 인덱스를선택 변수와 데이터를 확인 후
마지막에 기준에 맞는 변수를 선택 밴수와 교환

둘다 후지다

알고리즘은 교환횟수가 중요한게 아니라 비교횟수가 중요하다

quick Sort 가 가장빠르다

배열의 교차
배열의 방향이 기준에따른 근거를 논리에 따fk 세운다

플래그

프로그래밍에 많이 쓰인다
나머지(나머지에 해당되는 부분을 연산)
보수(최댓값에서 내려온다)
플래그(on, off식 연산)

Lookup 배열의 장점
배열의 크기가 커질수록 메모리 수는 증가하지만
선택의 속도는 일정하다