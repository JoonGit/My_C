Caller Callee 관계로 묶이는 겄을 Binding 이라고 한다
이 바인딩 과정은 컴파일 타임에서 만들어진다

Callee 가 매개 별수가 있을 경우 실인수를 꼭 규정해야 한다

Call by value

매개변수가 포인터(주소)면 Call by reference 이다

함수는 스택영역에서 쌓이고 나서 연산이 끝나면 반환후 스택에서 사라진다 이를 자동변수라 한다

재귀호출 = 반복물 + Stack자료구조  
비선형 자료구조에 쓰이며 트리 구조에 쓰인다
코드가 간결 해 지지만

call overhead
stack 증가
매개변수 복사
회복
이런 과정을 거치기에 효율이 좋지 않다
이를 해결하기 위해
매크로
인라인 함수가 나왔다