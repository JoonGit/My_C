고급이론
함수 그 이상의 것
컴파일러 최적화

컴파일은 디버그를 한후 릴리즈를 하는데
디버그는 되는데 릴리즈 하면 프로그램이 죽는 경우가 있다

최적화 -> 의존성
의존성을 변수를 기준으로
연산할때 변수의 의존성을 잘 파악해야 한다

최적화는 군살을 뺴는 것이다
디버그 에서는 모든 코드가 실행 되지만
릴리즈에서는 불필요하다고 컴파일러가 판단 되면 과정이 생략 된다

특정 변수(자료)에 대해 의존성이 존재하는 연산들을 구별

변수가 많은 것이 당연히 좋지 않지만 그 이유는
단순히 변수의 메모리를 차지하는것이 많다가 아닌
변수의 조합(경우의 수)가 많아 지기 때문에
변수는 적을 수록 좋다

최적화된 코드를 작성하기 위해 노력 <
컴파일러가 최적화 하기 좋게 작성

컴파일러는 기계언어 + Low 인데
high언어인 코드로 작성하기보단
컴파일러가 사용하는 코드로 작성을 하면 좋다

최적화 방해 요소
1. 변수가 많은 경우 -> 최대한 변수의 개수를 줄인다
2. 포인터 사용 주의(자제), c++ 에서는 포인터를 참조로 바꾸게 된다
참조 = 상수화 된 포인터

const를 잘 써야 좋다

심볼릭 상수
변경 가능성이 있으며 연산의 기준점이 되는 함수들을
의미가 명확해 보이는 상수로 만들어진 상수

포인터 상수화

형한정어
const
volatile

volatile는 컴파일러가 변수를 생략 되지 않고 변역을 하도록 하는 형한정어 이다

extern 외부변수
코드 분할시 함수를 외부 다른 파일에 저장하게 되는데
외부에 있는 변수를 사용하기 위해 외부 파일에 있는 이름과 같은 이름의 변수를 extern으로 선언해 사용한다

형식 재선언 : 자료형의 이름을 바꾸어 재 선언 하는일
사용하는 곳이 한정되어 있다
주의해서 사용해야 한다
형재선언은 생산성, 가독성을 떨어 뜨린다

정확한 기획 없이 사용하면 안된다

코딩 컨벤션