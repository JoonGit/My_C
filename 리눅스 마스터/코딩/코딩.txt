*나이출력
- 기준값을 변수로 하여 변경이 쉽게 한다

*두 수의 나눗셈
https://easy-coding.tistory.com/113
-round

*편지

*배열 뒤집기
reverse 함수

*삼각형의 완성조건 (1)

*문자열 뒤집기

*배열 자르기
-백터사용법

*배열의 유사도
foreach 사용법

*순서쌍의 개수
int answer = Enumerable.Range(1, n).Count(x => n % x == 0);

*문자 반복 출력하기
answer += new string(c, n);

*자릿수 더하기
answer += (int)Char.GetNumericValue(s[i]);